assembly-versioning-scheme: MajorMinorPatchTag
branches:
  master:
    is-release-branch: true
  develop:
    tag: beta
    increment: Minor
  feature:
    regex: ^issue?[\/-]
    tag: alpha-issue_{BranchName}
    increment: Minor